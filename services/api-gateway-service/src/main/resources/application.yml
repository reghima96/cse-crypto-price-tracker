spring:
  application:
    name: api-gateway-service
  config:
    import: optional:configserver:http://config-server:8888
  profiles:
    active: prod
management:
  endpoints:
    web:
      exposure:
        include: env